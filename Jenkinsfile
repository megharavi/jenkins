pipeline {
  agent any 
      stages{
      
          stage('build'){
            steps { echo "building stage........"
            }
          }
            
          stage ('test'){
            steps { echo "testing code.........."
            }
          }

          stage ('deploy'){
            steps {echo "deploying code........"
            }
          }
      }
  }
